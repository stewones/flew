{
  "scripts": {
    "start": "docusaurus start",
    "build": "rm -rf ./docs && cp -rf ../../docs ./docs && docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "vercel",
    "release": "npm run build && vercel --prod"
  },
  "dependencies": {
    "@cmfcmf/docusaurus-search-local": "^0.5.0",
    "@docusaurus/core": "2.0.0-alpha.70",
    "@docusaurus/preset-classic": "2.0.0-alpha.70",
    "classnames": "2.2.6",
    "react": "16.13.0",
    "react-dom": "16.13.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {}
}
