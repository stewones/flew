// default driver
export const R_DRIVER = 'http';
// default identifier name
// note that if you want to reach higher compatibility
// among the drivers, it should not collide with existing names
// like: id, _id or objectId
export const R_IDENTIFIER = 'doc_id';

//
// timestamp
export const R_TIMESTAMP_CREATED = 'created_at';
export const R_TIMESTAMP_UPDATED = 'updated_at';
