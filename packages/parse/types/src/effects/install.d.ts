/**
 * Bootstraps Parse on Flew
 *
 * @export
 * @param {*} config
 * @param {*} [sdk=Parse]
 * @returns {Parse} Parse instance configured
 */
export declare function install(config: any, sdk?: any): any;
