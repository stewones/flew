import { FlewBridge } from '../structurebridge';
import { FlewOptions } from '../structureoptions';
/**
 *
 *
 * @export
 * @param {string} [collection='']
 * @param {FlewOptions} [options={}]
 * @returns {FlewBridge}
 */
export declare function fetch(collection?: string, options?: FlewOptions): FlewBridge;
