import { FlewOptions } from '@flew/core';
import { FetchServer } from './server';
/**
 *
 *
 * @export
 * @param {string} [collection='']
 * @param {FlewOptions} [options={}]
 */
export declare function fetch(collection?: string, options?: FlewOptions): FetchServer;
